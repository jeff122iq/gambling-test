import { Promotion } from '@/mocks/promotions';
import { FC } from 'react';

type Props = Promotion;

export const Banner: FC<Props> = ({ id, img, status, description, title }) => {
  return <div></div>;
};
